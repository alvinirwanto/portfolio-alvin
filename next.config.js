/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'medium.com',
            'cdn-images-1.medium.com'
        ]
    }
}

module.exports = nextConfig
